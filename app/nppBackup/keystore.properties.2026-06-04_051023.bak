storeFile=S:/Coding/BingoNingo Android/BingoNingo/jcatkey.jks
storePassword=Sdfdf3120993085985
keyAlias=jcat key
keyPassword=Sdfdf3120993085985
```

Fill in your actual values — you'll know these from when you set up signing in Android Studio. Use forward slashes in the path.

Then make sure `keystore.properties` is in your `.gitignore` so it never gets pushed to GitHub:
```
keystore.properties
*.keystore